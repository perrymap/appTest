class Category {
  constructor(id, categoryTitle, count, imageUrl) {
    this.id = id;
    this.categoryTitle = categoryTitle;
    this.count = count;
    this.imageUrl = imageUrl;
  }
}

export default Category;
